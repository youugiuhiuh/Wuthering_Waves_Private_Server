[package]
name = "version-sync"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sync-version"
path = "src/main.rs"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"
toml = "0.8"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
