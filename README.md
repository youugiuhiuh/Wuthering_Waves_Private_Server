# Wuthering Waves Private Server Release

## Files
- `tgbot`: Telegram bot binary (Linux x86_64)
- `installer`: Installation script

## Quick Start
1. Download `installer`.
2. Grant execute permission: `chmod +x installer`.
3. Run: `./installer`.
